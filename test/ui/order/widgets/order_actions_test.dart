import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('should cancel', (tester) async {});

  testWidgets('should leave history', (tester) async {});

  testWidgets('show last without ordering', (tester) async {});

  testWidgets('no last to show', (tester) async {});

  testWidgets('show last with ordering and stashable', (tester) async {});

  testWidgets('show last with ordering and not stashable', (tester) async {});

  testWidgets('drop without ordering', (tester) async {});

  testWidgets('no stashed to drop', (tester) async {});

  testWidgets('drop with ordering and stashable', (tester) async {});

  testWidgets('drop with ordering and not stashable', (tester) async {});

  testWidgets('should stash', (tester) async {});

  testWidgets('should not stash if over limit', (tester) async {});
}
