import 'package:flutter/material.dart';

class MetaBlock extends StatelessWidget {
  const MetaBlock({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.symmetric(horizontal: 4),
      child: Text('•'),
    );
  }
}
