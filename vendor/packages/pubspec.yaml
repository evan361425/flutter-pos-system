name: packages
description: Bluetooth thermal printing package for POS system
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0 <3.42.0"

dependencies:
  flutter:
    sdk: flutter
  blue_thermal_printer: ^1.1.0
  esc_pos_utils_plus: ^2.0.4
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0